import 'package:flutter/material.dart';

class ZScorePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Z-Score')),
      body: Center(child: Text('Z-Score Page Content')),
    );
  }
}
