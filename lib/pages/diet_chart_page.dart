import 'package:flutter/material.dart';

class DietChartPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Suggest Diet Chart')),
      body: Center(child: Text('Suggest Diet Chart Page Content')),
    );
  }
}
